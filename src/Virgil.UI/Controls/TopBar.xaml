<UserControl x:Class="Virgil.UI.Controls.TopBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Border Background="{StaticResource Brush.Surface}" Padding="12" BorderBrush="{StaticResource Brush.AccentSoft}" BorderThickness="0,0,0,1">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBlock Text="Virgil — SUPERINTENDENT" Style="{StaticResource Txt.Title}"/>
      <StackPanel Orientation="Horizontal" Grid.Column="1">
        <Button Style="{StaticResource GhostButton}" Margin="8,0,0,0" Content="Minimize"/>
        <Button Style="{StaticResource GhostButton}" Margin="8,0,0,0" Content="Settings"/>
        <Button Style="{StaticResource GhostButton}" Margin="8,0,0,0" Content="Close"/>
      </StackPanel>
    </Grid>
  </Border>
</UserControl>
